# Agent instructions

The authoritative repository-wide instructions are in [`AGENTS.md`](../AGENTS.md).

Keep this directory for narrowly scoped agent playbooks only when a task needs
rules beyond the repository-wide delivery contract. Do not duplicate the root
contract here.
