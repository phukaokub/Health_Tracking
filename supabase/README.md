# Supabase local environment

Supabase is reserved for Auth, Postgres, private Storage, and local migrations. The CLI is not committed to the repository; install it separately, then discover the installed command surface with `supabase --help` before initializing the local project.

The first database migration belongs to Step 2 (Auth, profiles, and RLS). Do not add production credentials or personal Huawei data to this folder.

